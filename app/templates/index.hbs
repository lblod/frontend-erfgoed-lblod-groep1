{{#if (eq this.mode "")}}
  <Flow1::Index @address={{@model.address}} @search={{this.search}} />
{{else if (eq this.mode "search")}}
  <Flow1::Search @serializedAddress={{this.serializedAddress}} />
{{/if}}